{
  "name": "startuptechtalks.com",
  "private": true,
  "scripts": {
    "start": "next",
    "build": "next build && next export",
    "format": "prettier --write --ignore-path .gitignore '**/*.{js,md,mdx,html,css}'",
    "clean": "rm -rf .next"
  },
  "dependencies": {
    "next": "^9.0.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
